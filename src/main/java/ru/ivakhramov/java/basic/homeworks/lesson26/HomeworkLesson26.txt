    /**
     * 1. Создать классы Fruit, Apple extends Fruit, Orange extends Fruit
     *
     * 2. Создать класс Box, в который можно складывать фрукты. Коробки условно сортируются по типу фрукта:
     *    Box<Apple> только для яблок, Box<Orange> только для апельсинов, Box<Fruit> и для тех, и для других фруктов.
     *    Для хранения фруктов внутри коробки использовать ArrayList
     *
     * 3. Реализовать метод добавления фрукта в коробку
     *
     * 4. Реализовать метод weight, который высчитывает вес коробки
     *    (например, из веса одного фрукта и их количества, или может через суммирование, как посчитаем нужным).
     *    Вес фрукта задать самостоятельно, единицы измерения не важны
     *
     * 5. Реализовать метод compare, позволяющий сравнить текущую коробку с переданной в качестве аргумента.
     *    true – если их массы равны. Можно сравнивать коробки с разными типами фруктов
     *
     * 6. Реализовать метод, позволяющий пересыпать фрукты из текущей коробки в другую.
     *    Помнить про сортировку фруктов в коробках
     */